const OrderList = () => {
  return (
    <div>
      <h2>Orders lisg</h2>
      <button>add order</button>
      <button>delete order</button>
      <button>edit order</button>
    </div>
  );
};

export default OrderList;
